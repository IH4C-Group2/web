import type { FC } from "react";

const DriverIdInput: FC = () => {
    return (
        <div>
            <input type="text" name="DriverIdInput" className="border border-black"/>
        </div>
    );
};

export default DriverIdInput;