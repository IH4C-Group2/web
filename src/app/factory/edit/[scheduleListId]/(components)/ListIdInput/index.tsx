import type { FC } from "react";

const ListId: FC = () => {
    return (
        <div>
            <label>スケジュールID</label>
            <input type="text" name="ListId" />
        </div>
    );
};

export default ListId;